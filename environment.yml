name: base
channels:
  - defaults
dependencies:
  - pip:
    - mapbox==0.18.0
    - numpy-financial==1.0.0
    - plotly==5.0.0
    - pydeck==0.6.2
    - streamlit==0.84.0
prefix: C:\ProgramData\Anaconda3
